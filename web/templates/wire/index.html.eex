  <%= for wire <- @wires do %>
	  <article>
	  	<header>
	  		<a href="<%= wire.link %>"><%= wire.title %></a>
	  	</header>
	  	<p><%= wire.context %></p>
	  	<p><%= wire.user_id %></p>
	  </article>
  <% end %>

